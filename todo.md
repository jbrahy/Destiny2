# todo.md

Autogenerated by the portfolio overnight loop. On each visit the loop-worker takes
the FIRST unchecked item, builds it via a subagent (tests + security audit), commits
on the current branch, and appends the deploy command to DEPLOY_QUEUE.md (no deploy).
Then it marks the item [x] and the driver moves to the next project.

If there are no unchecked items, the loop-worker regenerates a grounded batch from
this repo's actual code (see REGEN_ROUNDS.md).
